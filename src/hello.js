import React from 'react';
import {View, Text} from 'react-native';
const hello = () => {
  return (
    <View style={{flex: 1, backgroundColor: 'yellow'}}>
      <Text style={{backgroundColor: 'red', marginTop: 90}}>hi</Text>
    </View>
  );
};

export default hello;
